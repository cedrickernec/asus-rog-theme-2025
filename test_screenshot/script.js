// Example JS syntax for theme showcase
const user = "Cédric";
let version = 2025;

function greet(name) {
  console.log(`Welcome ${name} to ASUS ROG Theme ${version}!`);
}

try {
  greet(user);
} catch (error) {
  console.error("Unexpected error:", error);
}
